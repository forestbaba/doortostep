import React from 'react'
import './style.scss'

const RounderCornerButton = ({ title }) => {
    return (
        <button className='round_corner'>{title}</button>
    )
}
export default RounderCornerButton;